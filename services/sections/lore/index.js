const cleanText = require('../../utils/cleanText');
module.exports = ($, url, name) => {
    // Helper function untuk membersihkan teks

    // Mengambil <p> pertama
    const introduction = "simple index text for testing only";
   
    // Output hasil
    return {
        name,
    
        introduction,
        
    };
};
